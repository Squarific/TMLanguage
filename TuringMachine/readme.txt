TuringMachine parser/converter/tester

Het programma geeft 1 of 0 terug voor een combinatie turingmachine (xml) en een te testen string.
Het kan ook een derde argument meegekrijgen om de turingmachine om te zetten in dot formaat.

Wanneer je het als library gebruikt kan je ook TM's als module samenvoegen.
